<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#ffffff">

    <!--<FrameLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:background="@drawable/circle"/>-->

    <FrameLayout
        android:id="@+id/image"
        android:layout_width="@dimen/emoji_width_height"
        android:layout_height="@dimen/emoji_width_height"
        android:layout_gravity="center"
        android:background="@drawable/emoji_default"/>

    <FrameLayout
        android:id="@+id/scroll"
        android:layout_width="@dimen/scroll_width_height"
        android:layout_height="@dimen/scroll_width_height"
        android:layout_gravity="center"/>

</FrameLayout>
